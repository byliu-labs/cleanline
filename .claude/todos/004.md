# #4 — Remove dead known_file_paths.json

**Problem:** `known_file_paths.json` was the original source of default read/write paths. Since tiers, `generate_config()` reads from `tiers.py` instead. The JSON file may be dead code.

**Current behavior:** `src/flow_state/known_file_paths.json` exists but `generate_config()` no longer loads it.

**Expected behavior:** Grep for references. If none, delete. If some, migrate callers to `tiers.py`.

**Test plan:**
- `grep -r "known_file_paths" src/ tests/` to find references
- Delete if clean. Run full test suite.

**Scope:** Delete one data file + any dead loader functions.
