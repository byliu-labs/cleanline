# #6 — flowstate export includes recommendedTier

**Status:** Done (2026-03-11)

**Resolution:** Implemented as part of the full `flowstate export` command. `build_profile()` in `export_cmd.py` reads tier from lockfile via `lockfile_mod.get_tier()` and sets `meta.recommendedTier`. Defaults to "balanced" when no tier is set. 48 tests cover the export command including `test_build_profile_recommended_tier`.
